openapi: 3.0.0
info:
  title: "AllTrails Users RESTful API"
  description: "ProfileService Microservice API for AllTrails"
  version: 1.0.0

# add security
security:
  - basicAuth: []

components:
  schemas:
    Users:
      type: object
    Admin:
      type: object
    CreateUserSchema:
      type: object
      description: Schema for creating a new user account.
      properties:
        username:
          type: string
          description: "Username of the user."
          example: "John Doe"
        email:
          type: string
          description: "Email of the user."
          example: "john@plymouth.ac.uk"
        password:
          type: string
          description: "Password for the user account."
    UpdateAccountSchema:
      type: object
      description: Schema for updating user account details.
      properties:
        username:
          type: string
          description: "Username of the user."
        email:
          type: string
          description: "Email of the user."
        password:
          type: string
          description: "Password for the user account."
        first_name:
          type: string
          description: "First name of the user."
        last_name:
          type: string
          description: "Last name of the user."
        dob:
          type: string
          format: date
          description: "Date of birth."
        phone_no:
          type: integer
          description: "Phone number."
        height:
          type: number
          format: double
          description: "Height in cm."
        weight:
          type: number
          format: double
          description: "Weight in kg."
        about_me:
          type: string
          description: "About me bio."
        marketing_language:
          type: string
          description: "Preferred marketing language."
        preferred_unit_metric:
          type: string
          description: "Preferred unit metric (metric/imperial)."
        time_preference_speed:
          type: string
          description: "Time preference for speed."
  securitySchemes:
    basicAuth:
      type: http
      scheme: basic
      x-basicInfoFunc: authenticator.authenticate_user
  parameters:
    email:
      name: email
      description: "User's email"
      in: path
      required: true
      schema:
        type: string
    password:
      name: password
      description: "User's password"
      in: path
      required: true
      schema:
        type: string

paths:
  /api/user:
    get:
      operationId: python_endpoints.user.getUser
      security:
        - basicAuth: []
      tags:
        - Users
      summary: "Show profile of a general user"
      responses:
        "200":
          description: "User profile retrieved successfully"
        "401":
          description: "Access denied"
        "404":
          description: "Error occured locating user"

    post:
      operationId: python_endpoints.user.createNewUser
      tags:
        - Users
      summary: "Create a account for new user"
      requestBody:
        description: "User to create"
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateUserSchema"
      responses:
        "201":
          description: "Account created"
        "409":
          description: Conflict (username or email already exists)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    enum: [USERNAME_EXISTS, EMAIL_EXISTS]
                  message:
                    type: string
        "400":
          description: Invalid data input

    put:
      operationId: python_endpoints.user.updateUser
      security:
        - basicAuth: []
      tags:
        - Users
      summary: "Update account details of the user"
      requestBody:
        description: "Updated user data"
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAccountSchema"
      responses:
        "404":
          description: User not found
        "200":
          description: "User account updated successfully"

    delete:
      operationId: python_endpoints.user.deleteUser
      security:
        - basicAuth: []
      tags:
        - Users
      summary: Delete this user account
      responses:
        "404":
          description: Could not find this user to delete
        "401":
          description: User issue
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    enum: [ACCESS_DENIED, CREDENTIALS_NOT_FOUND]
                  message:
                    type: string
        "204":
          description: User deleted successfully          

  /api/user/activities/{activity_name}:
    post:
      operationId: python_endpoints.user.addActivity
      security:
        - basicAuth: []
      tags:
        - Users
      summary: "Add an activity to user's profile"
      parameters:
        - name: activity_name
          in: path
          required: true
          schema:
            type: string
          description: "Name of the activity to add"
      responses:
        "201":
          description: "Activity added successfully"
        "404":
          description: "User not found or Activity not found"
        "409":
          description: "User already has this activity"
        "401":
          description: "Credentials not found or invalid"

  /api/admin/accounts:
    get:
      security:
        - basicAuth: []
      operationId: python_endpoints.administrator.getAllUsers
      tags:
        - Admin
      summary: "Show all user accounts"
      responses:
        "200":
          description: "Successfully retrieved all user accounts"
        "401":
          description: "Unauthorised access"        